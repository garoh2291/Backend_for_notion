{
  "title": "create task",
  "type": "object",
  "properties": {
    "body": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "minLength": 1
        },
        "status": {
          "type": "string",
          "enum": ["doing", "done","to do"]
        },
        "date": {
          "type": "string",
          "format": "date"
        }
      }
    }
  },
  "required": [
    "body"
  ]
}
